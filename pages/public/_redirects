# /@:handle is now handled by the apex Worker — it proxies through to
# the @lopecode/ledger notebook bundle (LEDGER_RKEY in src/worker.js)
# via the render Worker, keeping the URL bar at lopecode.com/@:handle.

# Stable shortlink to the atproto integration spec.
/spec  https://github.com/tomlarkworthy/lopecode-dev/blob/main/specs/atproto.md  302
